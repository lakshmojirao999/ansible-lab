---
-  name: Install fluentd 
   hosts: web
   become: true

   tasks:
    - name: Install fluentd agent #td-agent4
      raw: curl -L https://toolbelt.treasuredata.com/sh/install-redhat-td-agent4.sh | sh

    -   
  